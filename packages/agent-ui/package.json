{
  "name": "laptracker-agent",
  "version": "1.0.0",
  "description": "LapTracker9000 desktop agent",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . laptracker-agent --platform=win32 --arch=x64 --out=dist --overwrite --icon=favicon.ico --ignore=dist"
  },
  "dependencies": {
    "chokidar": "^3.6.0"
  },
  "devDependencies": {
    "electron": "^37.2.0",
    "electron-packager": "^17.1.2"
  }
}
